<view class="" hover-class="none" hover-stop-propagation="false" style="width: 100%; height: 100%;">
	<map id="map" longitude="{{longitude}}" latitude=" {{latitude}}" markers="{{markers}}" scale="13" show-location style="width: 100%; height: 100%;">
    <view class="margin">
			<van-search value="{{ value }}" placeholder="请输入关键词进行搜索" use-action-slot  bind:change="onChange" bind:search="onSearch">
				<view slot="action" bind:tap="onClick">搜索</view>
			</van-search>
		</view>
	</map>
	<!-- {{longitude}} -->
</view>